{
  "name": "deploy-toolkit",
  "version": "0.1.0",
  "description": "Deploy small vibecoded apps to Firebase with one command.",
  "type": "module",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test 'lib/**/*.test.mjs'"
  }
}
