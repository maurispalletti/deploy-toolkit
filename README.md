# deploy-toolkit

One command to deploy a small app to Firebase.

## Usage

```bash
./deploy-app .
```

Run from anywhere; pass the path to the app folder. The script asks a few plain-language questions and creates a Firebase project, configures the right services, and deploys.

## Status

v0.1 — work in progress.
